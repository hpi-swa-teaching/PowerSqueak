A PSDropIndicator is a thin horizontal bar shown in the slide sidebar while a slide is being dragged. It marks the snapped insertion position. Its `indent` controls how far from the left edge the bar starts, visually communicating which section nesting level a dropped slide would join.

Instance Variables
	indent		the nesting depth at which the bar starts (an Integer, 0 = flush left)
