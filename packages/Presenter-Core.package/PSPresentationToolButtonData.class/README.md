A PSPresentationToolButtonData creates the button data dictionaries used by PSPresentationTool.

Instance Variables
	tool:	<PSPresentationTool>

tool
	- The presentation tool for which button data is created